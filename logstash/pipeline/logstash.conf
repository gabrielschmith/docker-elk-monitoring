input {
	beats {
    	port => 5000
  	}
}

filter {
	json {
		source => "message"
		target => "log"
	}
	json {
		source => "log"
	}
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
	file {
		message_format => "%{[message]}"
		path => "/home/Log-%{+YYYY-MM-dd}.log"
	}
}