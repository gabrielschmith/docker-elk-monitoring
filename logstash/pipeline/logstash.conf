input {
	tcp {
		port => 5000
	}
}

filter {
	json {
		source => "message"
		target => "log"
		remove_field => [
			"message"
		]
	}
	json {
		source => "log"
	}
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
	}
}